{
  "checkpoints": {
    "american-put": {
      "atol": 1e-09,
      "inputs_code": "n = int(rng.integers(5, 9))\nu = float(rng.uniform(1.15, 1.8))\nd = float(rng.uniform(0.55, 0.9))\nr = float(rng.uniform(0.01, 0.1))\nS0 = float(rng.uniform(20.0, 150.0))\nK = float(S0 * rng.uniform(0.9, 1.3))\ninputs = (S0, u, d, r, n, K)\n",
      "kind": "exact",
      "seed": 106
    },
    "converged-price": {
      "holdout": "data/holdout_puts.csv",
      "kind": "metric",
      "metric": "rmse",
      "threshold": -0.04
    },
    "european-price": {
      "atol": 1e-09,
      "inputs_code": "n = int(rng.integers(4, 9))\nu = float(rng.uniform(1.15, 1.8))\nd = float(rng.uniform(0.55, 0.9))\nr = float(rng.uniform(0.01, 0.1))\nS0 = float(rng.uniform(20.0, 150.0))\nK = float(S0 * rng.uniform(0.8, 1.2))\ninputs = (S0, u, d, r, n, K)\n",
      "kind": "exact",
      "seed": 103
    },
    "exercise-boundary": {
      "atol": 1e-06,
      "inputs_code": "# A fifty-step CRR-calibrated put near the Wolfram demonstration's parameters.\nK = float(rng.uniform(40.0, 48.0))\nsigma = float(rng.uniform(0.2, 0.4))\ninputs = (40.0, K, 0.07, sigma, 3.0, 50)\n",
      "kind": "exact",
      "seed": 107
    },
    "hedge-walk": {
      "atol": 1e-09,
      "inputs_code": "n = int(rng.integers(5, 9))\nu = float(rng.uniform(1.15, 1.8))\nd = float(rng.uniform(0.55, 0.9))\nr = float(rng.uniform(0.01, 0.1))\nS0 = float(rng.uniform(20.0, 150.0))\nK = float(S0 * rng.uniform(0.8, 1.2))\ninputs = (S0, u, d, r, n, K, rng.integers(0, 2, size=n))\n",
      "kind": "exact",
      "seed": 105
    },
    "no-early-exercise-call": {
      "kind": "review"
    },
    "replicating-portfolio": {
      "atol": 1e-09,
      "inputs_code": "n = int(rng.integers(4, 9))\nu = float(rng.uniform(1.15, 1.8))\nd = float(rng.uniform(0.55, 0.9))\nr = float(rng.uniform(0.01, 0.1))\nS0 = float(rng.uniform(20.0, 150.0))\nK = float(S0 * rng.uniform(0.8, 1.2))\ninputs = (S0, u, d, r, n, K)\n",
      "kind": "exact",
      "seed": 104
    },
    "risk-neutral-prob": {
      "atol": 1e-12,
      "inputs_code": "u = float(rng.uniform(1.15, 1.8))\nd = float(rng.uniform(0.55, 0.9))\nr = float(rng.uniform(0.01, 0.1))\ninputs = (float(rng.uniform(20.0, 150.0)), u, d, r, int(rng.integers(3, 8)))\n",
      "kind": "exact",
      "seed": 102
    },
    "stock-lattice": {
      "atol": 1e-09,
      "inputs_code": "n = int(rng.integers(5, 9))\nu = float(rng.uniform(1.15, 1.8))\nd = float(rng.uniform(0.55, 0.9))\nr = float(rng.uniform(0.01, 0.1))\ninputs = (float(rng.uniform(20.0, 150.0)), u, d, r, n)\n",
      "kind": "exact",
      "seed": 101
    }
  },
  "content_version": "fd6371ba7dc993b3366d410163a764ff83e85c7b0f5efd0ffd04b20c97fa5eab",
  "lab": "quant/binomial-pricer-lab"
}
